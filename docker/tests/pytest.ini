[pytest]
# Add options to control pytest behavior
minversion = 6.0
addopts = -ra -q
testpaths =
    tests
    integration_tests
python_files = test_*.py
python_classes = Test
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    smoke: marks tests as smoke tests